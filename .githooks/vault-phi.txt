# Cortex vaults that handle PHI — the arming list for ADR-012 gate dimension 4.
#
# GENERATED by core/scripts/generate-vault-slugs.ts from the client registry
# (`PHI` column). Do not hand-edit; the next regeneration overwrites it.
#
# Full `cortex-<slug>` repo names, same form as vault-slugs.txt — so the
# anonymization sweep's slug rule recognises them and the two files stay
# readable side by side. The hook matches against $BASE.
# Carries no client names, no anon handles and no mapping.
#
# If this file is ABSENT the hook arms dimension 4 for EVERY vault. Dimension 4
# only warns, so the cost of over-arming is noise and the cost of under-arming
# is a missed patient identifier. It fails toward noise on purpose.
#
# 5 vault(s).

cortex-curalinc
cortex-lumate-health
cortex-platinum-dermatology
cortex-rhythm-healthcare
cortex-spear-pt
